

    {%- block form_errors -%}
        {%- if errors|length > 0 -%}
    <div class="erreur">
                {%- for error in errors -%}
                    <p>{{ error.message }}</p>
                {%- endfor -%}
    </div>
        {%- endif -%}
    {%- endblock form_errors -%}

